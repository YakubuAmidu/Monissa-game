var buttonColors = ['red', 'yellow', 'green'];
var gamePattern = [];

function nextSequence() {
  var randomNumber = Math.floor(Math.random() * 3);
  var randomChosenColor = randomNumber;
  randomChosenColor.buttonColors;
  randomChosenColor.push(gamePattern);
}
